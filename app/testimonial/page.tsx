import React from 'react'

const Testimonials = () => {
  return (
    <section id="testimonials" className="container-clamp spacing-clamp-xl text-center">
        <h2 className="text-clamp-4xl font-sans font-bold">Testimonials</h2>

        <p className="font-mono text-clamp-xl mx-auto leading-relaxed" style={{maxWidth: 'clamp(280px, 50vw, 600px)', marginTop: 'clamp(1.5rem, 2.5vw, 3rem)', marginBottom: 'clamp(1.5rem, 2.5vw, 3rem)'}}>
            Testimonial content coming soon...
          </p>
    </section>
  )
}

export default Testimonials
